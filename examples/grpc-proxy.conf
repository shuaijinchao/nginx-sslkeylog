# gRPC 配置示例
http {
    grpc_ssl_keylog on;
    grpc_ssl_keylog_file /var/log/nginx/grpc_ssl_keys.log;
    
    server {
        listen 80;
        location / {
            grpc_pass grpcs://backend.example.com:443;
        }
    }
} 