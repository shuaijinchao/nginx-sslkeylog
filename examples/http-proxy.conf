# HTTP 代理配置示例
http {
    proxy_ssl_keylog on;
    proxy_ssl_keylog_file /var/log/nginx/ssl_keys.log;
    
    server {
        listen 80;
        location / {
            proxy_pass https://backend.example.com;
        }
    }
} 